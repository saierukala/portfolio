# Public assets

Place your **resume PDF** here and name it:

```
resume.pdf
```

The "Download Resume" buttons on the site will then serve this file.  
If you use a different filename, update `resumePdf` in `src/data/resume.ts`.
